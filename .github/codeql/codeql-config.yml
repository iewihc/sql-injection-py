name: "Custom CodeQL Config"
queries:
  - uses: security-and-quality
  - uses: security-extended
query-filters:
  - exclude:
      problem.severity:
        - warning
        - recommendation
paths-ignore:
  - "**/tests/**"
  - "**/test/**"
max-paths-per-analysis: 10000
